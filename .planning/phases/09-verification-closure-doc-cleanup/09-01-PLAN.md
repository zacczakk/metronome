---
phase: 09-verification-closure-doc-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-tools-repo-rename/08-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - REPO-01
  - REPO-02
  - REPO-03
  - REPO-04
  - REPO-05
  - REPO-06
  - TOOL-01
  - TOOL-02
  - TOOL-03

must_haves:
  truths:
    - "08-VERIFICATION.md exists with pass/fail attestation for all 9 Phase 8 requirements"
    - "Each requirement attestation includes live evidence (actual command output or file inspection)"
    - "REQUIREMENTS.md REPO-01..06 checkboxes are checked [x]"
    - "REQUIREMENTS.md traceability table shows all 24 v2.0 requirements as Complete"
  artifacts:
    - path: ".planning/phases/08-tools-repo-rename/08-VERIFICATION.md"
      provides: "Formal Phase 8 verification attestation"
      contains: "REPO-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement checkboxes and traceability"
      contains: "[x] **REPO-01**"
  key_links:
    - from: ".planning/phases/08-tools-repo-rename/08-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "requirement IDs referenced in both"
      pattern: "REPO-0[1-6]|TOOL-0[1-3]"
---

<objective>
Run live verification checks for all 9 Phase 8 requirements and create formal 08-VERIFICATION.md attestation. Update REQUIREMENTS.md checkboxes and traceability table to reflect completion.

Purpose: Close the process/documentation gaps identified in v2.0-MILESTONE-AUDIT.md — the code is functionally complete, but formal verification never ran and REQUIREMENTS.md has 6 unchecked boxes.
Output: 08-VERIFICATION.md + updated REQUIREMENTS.md
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v2.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run live verification checks and create 08-VERIFICATION.md</name>
  <files>.planning/phases/08-tools-repo-rename/08-VERIFICATION.md</files>
  <action>
Run live checks for all 9 Phase 8 requirements and create 08-VERIFICATION.md in the Phase 8 directory with a pass/fail evidence table.

**REPO-01** (repo folder renamed):
- Run: `ls -d ~/Repos/acsync` — confirm exists
- Run: `ls ~/Repos/agents 2>&1` — confirm does NOT exist
- Expected: acsync exists, agents does not

**REPO-02** (all internal path refs updated):
- Run: `rg "~/Repos/agents" src/ configs/ 2>/dev/null | head -5` — confirm zero matches
- Expected: no output (zero refs in source/configs)

**REPO-03** (binary re-registered):
- Run: `which acsync` — confirm binary on PATH
- Run: `acsync --version` or `acsync --help` from `/tmp` — confirm works from any cwd
- Expected: binary resolves and executes

**REPO-04** (push propagates path changes):
- Run: `acsync check --pretty 2>&1 | head -20` — confirm no errors
- Run: `rg "~/Repos/agents" ~/.claude/CLAUDE.md ~/.config/opencode/AGENTS.md ~/.gemini/AGENTS.md ~/.codex/AGENTS.md 2>/dev/null` — confirm zero refs in targets
- Expected: check succeeds, zero stale refs in target files

**REPO-05** (OpenCode instructions path):
- Run: `cat ~/.config/opencode/opencode.json | grep -A2 instructions` — confirm points to AGENTS.md
- Expected: instructions array contains AGENTS.md path

**REPO-06** (stale files cleaned):
- Run: `ls ~/.config/opencode/OPENCODE.md ~/.gemini/GEMINI.md ~/.codex/instructions.md 2>&1` — confirm all absent
- Expected: "No such file" for all 3

**TOOL-01** (TOOLS.md exists):
- Run: `ls -la configs/instructions/TOOLS.md` — confirm exists with content
- Expected: file exists, non-empty

**TOOL-02** (AGENTS.md references TOOLS.md):
- Run: `grep "TOOLS.md" configs/instructions/AGENTS.md` — confirm absolute path reference
- Expected: reference to ~/Repos/acsync/configs/instructions/TOOLS.md

**TOOL-03** (TOOLS.md not rendered):
- Run: `rg "TOOLS.md" src/ --glob '!*.test.*' | grep -i render` — confirm no rendering logic
- Expected: no render references

Format as a markdown table:

```
| Requirement | Check | Result | Evidence |
|-------------|-------|--------|----------|
| REPO-01     | ...   | PASS   | ...      |
```

Include header with date, phase reference, and overall status.
  </action>
  <verify>
- `cat .planning/phases/08-tools-repo-rename/08-VERIFICATION.md` shows all 9 requirements with PASS results
- File contains actual command output as evidence, not just claims
  </verify>
  <done>08-VERIFICATION.md exists with live evidence for REPO-01..06 and TOOL-01..03, all showing PASS</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md checkboxes and traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read `.planning/REQUIREMENTS.md` and make these updates:

1. **Check REPO-01..06 boxes**: Change `- [ ] **REPO-01**` through `- [ ] **REPO-06**` to `- [x] **REPO-01**` through `- [x] **REPO-06**`

2. **Update traceability table**: Change all 9 rows for TOOL-01..03 and REPO-01..06 from `Pending` to `Complete`. Specifically:
   - `| TOOL-01 | Phase 9 | Pending |` → `| TOOL-01 | Phase 8 | Complete |`
   - `| TOOL-02 | Phase 9 | Pending |` → `| TOOL-02 | Phase 8 | Complete |`
   - `| TOOL-03 | Phase 9 | Pending |` → `| TOOL-03 | Phase 8 | Complete |`
   - `| REPO-01 | Phase 9 | Pending |` → `| REPO-01 | Phase 8 | Complete |`
   - Same for REPO-02 through REPO-06
   Per user decision: credit Phase 8 (did the work), not Phase 9.

3. **Update metadata**: Set 'Last updated' date to today (2026-02-24).

Do NOT modify the v1 requirements section or future requirements section.
  </action>
  <verify>
- `grep -c "\[ \]" .planning/REQUIREMENTS.md` returns 0 (no unchecked v2 boxes)
- `grep "REPO-01" .planning/REQUIREMENTS.md` shows `[x]` and `Complete`
- `grep "Pending" .planning/REQUIREMENTS.md` returns 0 for v2 requirements
  </verify>
  <done>All 24 v2.0 requirement checkboxes are [x], traceability table shows Complete for all 24, Phase attribution correct (Phase 8 for TOOL/REPO)</done>
</task>

</tasks>

<verification>
- 08-VERIFICATION.md exists in Phase 8 directory with 9/9 PASS results
- REQUIREMENTS.md has zero unchecked v2.0 boxes
- REQUIREMENTS.md traceability has zero Pending v2.0 rows
- All evidence is from live command output, not copied from audit doc
</verification>

<success_criteria>
- 08-VERIFICATION.md created with live evidence for all 9 requirements
- REQUIREMENTS.md fully updated: checkboxes, traceability, dates
- Zero process gaps remain for Phase 8 requirements
</success_criteria>

<output>
After completion, create `.planning/phases/09-verification-closure-doc-cleanup/09-01-SUMMARY.md`
</output>
