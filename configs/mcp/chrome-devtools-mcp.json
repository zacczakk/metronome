{
  "description": "Chrome DevTools MCP — attaches to running Chrome 144+ via --autoConnect",
  "transport": "stdio",
  "command": "npx",
  "args": ["-y", "chrome-devtools-mcp@latest", "--autoConnect", "--no-usage-statistics"],
  "enabled": true,
  "disabled_for": ["codex"]
}
