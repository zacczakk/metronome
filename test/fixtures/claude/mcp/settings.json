{
  "mcpServers": {
    "tavily": {
      "command": "python",
      "args": [
        "-m",
        "tavily_mcp"
      ],
      "env": {
        "TAVILY_API_KEY": "${TAVILY_API_KEY}",
        "UPTIMIZE_ENV": "dev"
      }
    },
    "context7": {
      "type": "http",
      "url": "https://mcp.context7.com/mcp",
      "headers": {
        "CONTEXT7_API_KEY": "${CONTEXT7_API_KEY}"
      }
    }
  }
}
